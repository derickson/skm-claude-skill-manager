stop the servers Find and stop any running Python HTTP server processes. Use `pkill -f "python3 -m http.server"` or equivalent to terminate them. Confirm which process(es) were stopped, including the port they were running on.
```

**Usage:**
```
/serve-stop
