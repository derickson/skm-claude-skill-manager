Serve the directory $ARGUMENTS using Python's built-in HTTP server. Run `python3 -m http.server 8000 --directory <path>` where `<path>` is the specified directory. If no port is available on 8000, try 8080, then 8888. Print the URL where the server is accessible.
```

**Usage:**
```
/serve @src/public
